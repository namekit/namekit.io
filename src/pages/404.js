import React from 'react';
import Layout from '../components/Layout';

const NotFoundPage = () => (
  <Layout darkText>
    <h1>Page Not Found</h1>
    <p>We couldn't find what you were looking for, perhaps try something else.</p>
  </Layout>
);

export default NotFoundPage;
